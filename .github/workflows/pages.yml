name: Deploy Gem V6 training site to Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  deploy:
    permissions:
      contents: read
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Check training site invariants
        run: python3 tests/check_site.py

      - name: Stage public training materials only
        run: |
          set -eu
          mkdir -p _site/assets _site/downloads
          cp index.html .nojekyll _site/
          cp assets/jata-negara.svg assets/mohamad-nurakmal.png assets/slide-gems-qr.png _site/assets/
          for file in Markah_Risiko_4x4.pdf Panduan_7_Kategori_Isu_Audit_V6.txt Panduan_Format_Analisis_Audit_V6_Kod_Tajuk.txt Instruction_Gem_Penganalisis_Audit_V6_Kod_Tajuk.txt Prompt_Kali_Pertama_Analisis_V6_Kod_Tajuk.txt Contoh_Latihan_Sintetik_V6.md Templat_Fokus_Pengurusan_V6.md manifest-v6.json; do
            cp "downloads/$file" _site/downloads/
          done

      - name: Configure Pages
        uses: actions/configure-pages@v6

      - name: Upload reviewed site
        uses: actions/upload-pages-artifact@v5
        with:
          path: _site

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
